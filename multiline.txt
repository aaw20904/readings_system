CREATE TABLE `my_bot`.`district` (
  `district_id` BIGINT NOT NULL AUTO_INCREMENT,
  `district` VARCHAR(45) NOT NULL,
  PRIMARY KEY (`district_id`),
  UNIQUE INDEX `district_UNIQUE` (`district` ASC) VISIBLE);